export const sum = (a, b) => {
	return a + b;
}; //Esto es un módulo

export const multiply = (a, b) => {
	return a * b;
}; //Esto es otro módulo

//Exportar varios modulos de forma antigua
// module.export = {
// 	sum,
// 	multiply,
// };

//exportar solo 1 funcion
// export default sum; exportar solo 1 funcion
