import { sum, multiply } from './utilsMath.js'; //para export normales
// import sum from './utilsMath.js'; //para los export default

//llamo la funcion y capturo el valor en una variable (value), luego le paso los valores: 2 y 2
const value = sum(2, 2);
//imprimo value en consola

console.log(value);
